@{
    var grid = Html.DevExpress().GridView(settings =>
    {
        settings.Name = "GridViewParcelas";
        settings.Width = System.Web.UI.WebControls.Unit.Percentage(100);
        settings.Height = System.Web.UI.WebControls.Unit.Percentage(100);
        settings.CallbackRouteValues = new { Controller = "POM", Action = "GridViewPartialParcelas" };

        //settings.SettingsEditing.AddNewRowRouteValues = new { Controller = "POM", Action = "GridViewPartialParcelasAddNew" };
        settings.SettingsEditing.UpdateRowRouteValues = new { Controller = "POM", Action = "GridViewPartialParcelasUpdate" };
        settings.SettingsEditing.DeleteRowRouteValues = new { Controller = "POM", Action = "GridViewPartialParcelasDelete" };
        settings.SettingsEditing.Mode = GridViewEditingMode.EditForm;
        settings.SettingsBehavior.ConfirmDelete = true;

        settings.CommandColumn.Visible = true;
        //settings.CommandColumn.ShowNewButton = true;
        settings.CommandColumn.ShowDeleteButton = true;
        settings.CommandColumn.ShowEditButton = true;

        settings.KeyFieldName = "CODIGO";

        settings.SettingsPager.Visible = true;
        settings.Settings.ShowGroupPanel = false;
        settings.Settings.ShowFilterRow = true;
        settings.Settings.ShowFilterRowMenu = true;
        settings.SettingsBehavior.AllowSelectByRowClick = false;
        settings.CommandColumn.ShowApplyFilterButton = false;
        settings.SettingsBehavior.FilterRowMode = GridViewFilterRowMode.Auto;
        settings.CommandColumn.ShowClearFilterButton = true;

        settings.SettingsAdaptivity.AdaptivityMode = GridViewAdaptivityMode.HideDataCellsWindowLimit;
        settings.SettingsAdaptivity.AdaptiveColumnPosition = GridViewAdaptiveColumnPosition.Left;
        settings.SettingsAdaptivity.AdaptiveDetailColumnCount = 1;
        settings.SettingsAdaptivity.AllowOnlyOneAdaptiveDetailExpanded = false;
        settings.SettingsAdaptivity.HideDataCellsAtWindowInnerWidth = 0;

        settings.Columns.Add(c =>
        {
            c.FieldName = "CODIGO";
            c.Caption = "CODIGO";
            c.ReadOnly = true;
            c.Settings.AutoFilterCondition = AutoFilterCondition.Contains;
        });

        settings.Columns.Add(c=>
        {
            c.FieldName = "NOMBRE";
            c.Settings.AutoFilterCondition = AutoFilterCondition.Contains;
        });

        settings.Columns.Add(c =>
        {

            settings.Name = "ComboVariedad";
            c.FieldName = "VARIEDAD";
            c.Caption = "VARIEDAD";
            c.Settings.AutoFilterCondition = AutoFilterCondition.Contains;
        });

        settings.Columns.Add(column =>
        {
            column.FieldName = "FINCA";
            column.ReadOnly = true;
            column.Settings.AutoFilterCondition = AutoFilterCondition.Contains;
        });

        settings.CustomJSProperties = (s, e) =>
        {
            if (ViewData[POM.Controllers.POMController.EditResultKey] != null)
            {
                e.Properties["cpMessage"] = ViewData[POM.Controllers.POMController.EditResultKey].ToString();
            }
        };

        settings.ClientSideEvents.EndCallback = "OnEndCallback";
    });
    if (ViewData["EditError"] != null)
    {
        grid.SetEditErrorText((string)ViewData["EditError"]);
    }
}
@grid.Bind(Model).GetHtml()

@functions {
    GridViewEditingMode EditMode { get { return ViewBag.EditMode; } }
}